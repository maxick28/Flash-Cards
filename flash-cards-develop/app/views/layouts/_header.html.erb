
  <%# <div class="shadow-lg p-3 mb-5 bg-white rounded">Larger shadow</div> %>
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow-lg">
  <div class="container-fluid">
    <div class="container-fluid">
    <%= link_to image_tag('logo.svg'), root_path, class: "navbar-brand" %>
    <%= link_to 'FlashCards', root_path, class: "navbar-brand" %>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <%= link_to 'Collections', collections_path, class: "nav-link active" %>
        <%= link_to 'About', about_path, class: "nav-link" %>
      </div>
      <div class="navbar-nav ml-auto">
      <div class="collapse navbar-collapse" id="navbarNavDarkDropdown">
      <ul class="navbar-nav">
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle mr-1" href="#" id="navbarDarkDropdownMenuLink" role="button" data-toggle="dropdown" aria-expanded="false">
            Categories
          </a>
          <ul class="dropdown-menu dropdown-menu-dark" aria-labelledby="navbarDarkDropdownMenuLink">
            <% Category.all.each do |category| %>
              <li><%= link_to category.name, collections_my_filter_path({category_id: category.id}), class: 'dropdown-item' %></li>
            <% end %>
          </ul>
        </li>
      </ul>
      </div>
      <% if current_user %>
        <%= link_to 'Profile', user_path(current_user), class: "btn btn-outline-light" %>
        <%= link_to 'Sign out', destroy_user_session_path, method: :delete, class: "btn btn-outline-light mx-2" %>
      <% else %>
        <%= link_to 'Log in', new_user_session_path, class: "btn btn-outline-light" %>
        <%= link_to 'Sign up', new_user_registration_path, class: "btn btn-light mx-2" %>
      <% end %>
      </div>
    </div>
  </div>
</nav>
