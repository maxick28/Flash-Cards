<div class='container'>
<h1><%= pluralize(@users.size, "User") %></h1>
<ul>
  <% @users.each do |user| %>
    <li>
      <%= link_to user.name, users_show_path %>
      created
      <%= time_ago_in_words(user.created_at) %> ago
    </li>
  <% end %>
</ul>
</div>
